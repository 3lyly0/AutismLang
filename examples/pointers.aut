fn main():
    # Memory allocation
    ptr<void> x = alloc(8)
    ptr<int> xi = ptr<int>(x)
    unsafe:
        *xi = 42
        print(*xi)
    
    # Address-of operator
    int y = 100
    ptr<int> p = &y
    unsafe:
        print(*p)
    
    # Free memory
    free(x)
    
    print(0)