{
    "version": "0.6.0",
    "backend": "c-static",
    "changes": [
        "Unsafe block safety model for raw pointer dereference",
        "Dereference outside unsafe is rejected with UnsafeError",
        "ptr<void> dereference is forbidden and requires explicit typed cast",
        "Strict static typing for int, bool, str, and pointers",
        "Kernel/freestanding output mode via --no-runtime"
    ]
}