{
    "version": "0.5.0",
    "backend": "c-static",
    "changes": [
        "Typed pointer system: ptr<T> declarations and pointer casts",
        "Strict static typing for int, bool, str, and pointers",
        "Compile-time TypeError diagnostics with line numbers",
        "Kernel/freestanding output mode via --no-runtime",
        "No-runtime entrypoint and external hook ABI for low-level targets"
    ]
}